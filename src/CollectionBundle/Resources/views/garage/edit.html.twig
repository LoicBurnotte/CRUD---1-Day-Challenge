{% extends '@Collection/layout.html.twig' %}

{% block title %}Garage Edit{% endblock %}
{% block stylesheets %}{% endblock %}

{% block body %}
    <div class="container text-center">
        <div class="row">
            <div class="col-sm-6 offset-3">
                <h1>Edit Garage :</h1>
                <div class="newCar">
                    {{ form_start(form) }}
                    {{ form_errors(form) }}
                    {{ form_rest(form) }}
                    {{ form_end(form)}}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript %}{% endblock %}
